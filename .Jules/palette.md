## 2026-06-01 - [ARIA Labels Language Consistency]
 **Learning:** When adding ARIA labels to a localized application (like this Spanish PHP guide), screen readers expect the labels to match the primary language of the application to synthesize the voice correctly.
 **Action:** Always match the language of ARIA labels to the surrounding UI text.
